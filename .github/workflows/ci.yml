name: CI workflow

on:
    push:

jobs:
    build: # job name
        runs-on: ubuntu-latest # Github Runner (free)
        steps:
            - name: Checkout code
              uses: actions/checkout@v4 # clone the code in the github runner

            - name: Setup docker
              uses: docker/setup-buildx-action@v4

